<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="VocbularyTutor.MainWindow"
        Title="MainWindow" Height="480" Width="640" AllowsTransparency="True" WindowStyle="None" Background="{x:Null}">
   <Window.Resources>
    <LinearGradientBrush x:Key="SelectedBorderBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="Gray" Offset="0.965"/>
                <GradientStop Color="WhiteSmoke" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>
    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <DockPanel>
                        <Border
                            Panel.ZIndex="50"
                            Margin="0,100,-1,0" 
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            BorderThickness="1">
                            <TabPanel
                                Margin="0,0,0,0"
                                IsItemsHost="True" />
                        </Border>
                        <Border
                            BorderBrush="{x:Null}"
                            BorderThickness="1"
                            Background="#FFE9EDDF" >
                            <ContentPresenter 
                                ContentSource="SelectedContent" />
                        </Border>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="{x:Type TabItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Grid>
                        <Border Name="Border" 
                            Background="#FFAAAAAA"
                            CornerRadius="7,0,0,0"
                            BorderBrush="Gray"
                            BorderThickness="0,0,0,0"
                            Panel.ZIndex="50"
                            Margin="0,0,0,0"
                                >

                            <ContentPresenter x:Name="ContentSite"             
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                ContentSource="Header"
                                Margin="10,10,10,10"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Panel.ZIndex" Value="100" />
                            <Setter Property="Margin" Value="0,0,-2,0" />
                            <Setter TargetName="Border" 
                                    Property="BorderBrush" 
                                    Value="{StaticResource SelectedBorderBrush}"/>
                            <Setter TargetName="Border" 
                                Property="Background" 
                                Value="#FFE9EDDF" />
                            <Setter TargetName="Border" 
                                Property="CornerRadius" 
                                Value="0,0,0,0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</Window.Resources>
    <Grid>
    	<Grid.RowDefinitions>
    		<RowDefinition Height="0.15*"/>
    		<RowDefinition Height="0.85*"/>
    	</Grid.RowDefinitions>
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition Width="0.6*"/>
    		<ColumnDefinition Width="0.4*"/>
    	</Grid.ColumnDefinitions>
        <Rectangle Grid.ColumnSpan="2" Grid.RowSpan="2" Fill="#FF5B6D7C"></Rectangle>
        <Button Name="ExitButton" Grid.Column="1" Margin="194,12,12,15" Click="ExitButton_Click" />
        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal" Margin="0,0,10.88,0"/>
    	<TabControl Name="_menuTabControl" TabStripPlacement="Left" Grid.Row="1" Grid.ColumnSpan="2" Background="{x:Null}" Opacity="0.8" d:LayoutOverrides="GridBox">
        <TabItem Name="_tabItem1" Header="Пользователь" Foreground="#FF03B206" FontSize="29.333" FontFamily="Calibri" >
        	<Grid Height="373" />
		</TabItem>
        <TabItem Name="_tabItem2" Header="Словарь" Foreground="#FF03B206" FontSize="29.333" FontFamily="Calibri" Background="{x:Null}" >
            <Grid />
        </TabItem>
        <TabItem Name="_tabItem3" Header="Тесты" Background="{x:Null}" Foreground="#FF03B206" FontSize="29.333" FontFamily="Calibri" >
            <Grid />
        </TabItem>
		        <TabItem Name="_tabItem4" Header="Результаты" Background="{x:Null}" Foreground="#FF03B206" FontSize="29.333" FontFamily="Calibri">
            <Grid />
        </TabItem>
    </TabControl>
    </Grid>
</Window>
